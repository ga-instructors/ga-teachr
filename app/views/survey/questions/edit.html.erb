<h1>Editing Survey Question</h1>

<%= form_for(@survey_question) do |form| %>
  <%= render form %>
  <div class="actions">
    <%= form.submit %>
    <%= link_to 'Cancel', survey_questions_path %>
  </div>

<% end %>
