<div class="large container">
	<h1>Editing Cohort</h1>

	<%= form_for(@cohort) do |form| %>
		<%= render form %>
		<div class="actions">
			<%= form.submit %>
			<%= link_to 'Cancel', @cohort %>
		</div>
	<% end %>

</div>
