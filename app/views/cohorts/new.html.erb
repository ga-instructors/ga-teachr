<div class="large container">
	<h1>New Cohort</h1>

	<%= form_for(@cohort) do |form| %>
		<%= render form %>
		<div class="actions">
			<%= form.submit %>
			<%= link_to 'Cancel', cohorts_path %>
		</div>
	<% end %>

</div>
