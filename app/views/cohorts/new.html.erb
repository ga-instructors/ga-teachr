
<div class="medium container">
	<h1>New Cohort</h1>
	<%= render @cohort %>

	<%= form_for(@cohort) do |form| %>
		<%= render form %>
		<div class="actions">
			<%= form.submit %>
			<%= link_to 'Cancel', cohorts_path %>
		</div>
	<% end %>

</div>
