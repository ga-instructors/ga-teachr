$(function() {
  $('#students_content').html('<%=j render 'student_billboard', formats: [:html] %>');
  $('.student').removeClass('selected');
  $('#<%= dom_id(@student) %>').addClass('selected');
})
